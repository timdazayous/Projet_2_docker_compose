[project]
name = "app_front"
version = "0.1.0"
description = "Streamlit Frontend for User Data"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "streamlit",
    "requests",
    "pandas"
]
